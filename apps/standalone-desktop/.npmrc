# Standalone, isolated install for the remote-only desktop client.
# Detached from the repo workspace so installing here never touches the
# main workspace's root package-lock.json / node_modules.
ignore-scripts=true
audit=false
fund=false
package-lock=true
save-exact=true
